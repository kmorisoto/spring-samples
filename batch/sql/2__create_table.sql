CREATE TABLE IF NOT EXISTS batch.employee
(
    id     INT PRIMARY KEY,
    name   VARCHAR(50),
    age    INT,
    gender INT
);

CREATE TABLE IF NOT EXISTS batch2.employee
(
    id     INT PRIMARY KEY,
    name   VARCHAR(50),
    age    INT,
    gender INT
);
